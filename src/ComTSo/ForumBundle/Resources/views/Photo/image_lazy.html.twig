{% set filter = filter is defined and filter ? filter : 'preview' %}
<img class="lazy" data-original="{{ photo.filename | imagine_filter(filter) }}" alt="{{ photo.filename }}" title="{{ photo.title }}" {{ image_size_attrs(photo, filter) }} />
